1. navigate to server folder.
2. run: go run .\node.go
3. Now a popup will ask you to allow the server to run. Click allow.
4. It will now look like nothing is happening, but that is because all of the logs go straight into the node.log file. here you can see the logs. you can wait about 10 seconds before all of the logs are there.